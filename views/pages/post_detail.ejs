<!DOCTYPE html>
<html>
<head>
  <% include ../partials/header.ejs %>

  <script type="text/jsx">
    var blog_id = 2;
    var api_prefix = '<%= RBP.api_prefix %>';

    $(document).ready(function() {
        var blog_detail_url = api_prefix + 'api/post/' + blog_id;
        get_url_async(blog_detail_url).then(function(response){
            var blog_detail = JSON.parse(response);
            console.log(blog_detail);
            $('#latest_blog').removeClass('spinner');

            /*
            ReactDOM.render(
              <h1> {blog_detail} </h1> document.getElementById('latest_blog')
              )
              */
            }, function(err) {
              $('#latest_blog').removeClass('spinner');
              console.error('Failure', err);
        });
    });

  </script>
</head>

<body>
<% include ../partials/nav.ejs %>
<div class="jumbotron text-center">
    <div class="container">
      <h1> WIP </h1>
    </div>
</div>
<div class="container">
  <div id="latest_blog" class="spinner"></div>
</div>

</body>